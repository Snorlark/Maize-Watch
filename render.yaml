services:
  # Main Application (Node.js + Python)
  - type: web
    name: maize-watch-app
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        value: mongodb+srv://username:password@cluster.mongodb.net/maizewatch
      - key: ANALYTICS_SERVICE_URL
        value: http://localhost:8000
      - key: REDIS_URL
        fromService:
          type: redis
          name: maize-watch-redis
          property: connectionString
      - key: THINGSPEAK_CHANNEL_ID
        value: 3083651
      - key: THINGSPEAK_API_KEY
        value: YOUR_THINGSPEAK_API_KEY
      - key: JWT_SECRET
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: PYTHONUNBUFFERED
        value: 1

  # Redis Cache
  - type: redis
    name: maize-watch-redis
    plan: starter
    maxmemoryPolicy: allkeys-lru